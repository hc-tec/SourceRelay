{
  "name": "@intelligence/collector-extension",
  "version": "0.4.18",
  "private": true,
  "type": "module",
  "description": "Manifest V3 core for user-approved, read-only browser collection tasks.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "setup:build-browser": "playwright install chromium",
    "typecheck": "tsc --noEmit",
    "verify:transcript-capture": "node scripts/verify-transcript-capture.mjs",
    "verify:artifacts": "node scripts/verify-build-artifacts.mjs",
    "verify:extension-load": "node scripts/verify-extension-load.mjs",
    "verify:build": "npm run typecheck && npm run verify:transcript-capture && npm run build && npm run verify:artifacts && npm run verify:extension-load"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.330",
    "esbuild": "^0.25.0",
    "playwright": "^1.61.1",
    "typescript": "^5.8.0"
  }
}
