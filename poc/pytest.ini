[pytest]
testpaths = collector-python-client/tests
pythonpath = collector-python-client/src
addopts = -ra

# Historical Python POCs keep their own dependencies and test commands. They
# are intentionally not collected as part of Collector Core's default pytest
# invocation.
